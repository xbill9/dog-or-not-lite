# Four direct dependencies. Bounded at the next major rather than pinned exactly:
# this is a weekend build with no lockfile, and a caret range is honest about
# that where a fake pin would not be.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
boto3>=1.35,<2
pydantic>=2.7,<3
